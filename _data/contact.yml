# Email is NOT stored as plain text in the built HTML — only these byte values appear.
# To change it: run  python3 -c "print(list('you@example.com'.encode()))"
# and paste the list below (keeps naive mailto scrapers from matching a full address string).
# Current value: a.jha@tu-braunschweig.de
email_char_codes: [97, 46, 106, 104, 97, 64, 116, 117, 45, 98, 114, 97, 117, 110, 115, 99, 104, 119, 101, 105, 103, 46, 100, 101]
